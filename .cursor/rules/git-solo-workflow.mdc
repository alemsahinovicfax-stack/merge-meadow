---
description: Solo git workflow — master branch, English on remote, no PRs by default — vidi CLAUDE.md
alwaysApply: true
---

# Git — solo developer

Puna pravila: **`CLAUDE.md`** § "Git — solo (master)" (root repoa, kanonski).

Kratko: sve na `master`, bez feature grana/PR-ova osim na eksplicitan zahtjev, commit samo kad korisnik traži, engleski na remote.
