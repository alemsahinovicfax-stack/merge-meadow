---
description: Workflow za dokumentaciju i faze razvoja — vidi CLAUDE.md
alwaysApply: true
---

# Dokumentacija razvoja — workflow

Puna pravila: **`CLAUDE.md`** § "Početak sesije" i § "Scope guard" (root repoa, kanonski — ne duplicirati ovdje).

Kratko: pročitaj `docs/06-production/CHECKPOINT.md` frontmatter → `b0_aktivan` → prva `[ ]` u aktivnoj sekciji = sljedeći korak → jedan checkpoint korak po sesiji → na kraju ažuriraj CHECKPOINT frontmatter + `changelog.md`.
