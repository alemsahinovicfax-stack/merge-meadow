---
description: Godot asset import — novi PNG/SVG mora biti importan prije load(); Figma workflow — vidi CLAUDE.md
globs: game/assets/**/*
alwaysApply: false
---

# Godot asset import

Puna pravila: **`CLAUDE.md`** § "Novi asset u game/assets/ (C2+)" (root repoa, kanonski).

Kratko: `.\scripts\godot-import.ps1` nakon novog assetа, commitaj `*.import` uz raw fajl, `PipAssets.get_texture()` + fallback, headless `ResourceLoader.exists()` provjera prije playtesta.
